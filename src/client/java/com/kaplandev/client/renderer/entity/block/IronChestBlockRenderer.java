package com.kaplandev.client.renderer.entity.block;

/*import com.kaplandev.entity.block.IronChestBlockEntity;
import com.kaplandev.entity.block.PvpSpawnerBlockEntity;
import net.minecraft.client.MinecraftClient;
import net.minecraft.client.render.VertexConsumerProvider;
import net.minecraft.client.render.block.entity.BlockEntityRenderer;
import net.minecraft.client.render.block.entity.BlockEntityRendererFactory;
import net.minecraft.client.util.math.MatrixStack;

public class IronChestBlockRenderer implements BlockEntityRenderer<IronChestBlockEntity> {
    private final MinecraftClient client = MinecraftClient.getInstance();

    public IronChestBlockRenderer(BlockEntityRendererFactory.Context context) {
        // Renderer için context alınır, gerekli ise burada render dispatcher gibi şeyler alınabilir
    }

    @Override
    public void render(
            IronChestBlockEntity entity,
            float tickDelta,
            MatrixStack matrices,
            VertexConsumerProvider vertexConsumers,
            int light,
            int overlay
    ) {
        if (entity.getWorld() == null) return;
    }
}

*/